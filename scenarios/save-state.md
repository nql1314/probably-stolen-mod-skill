# 场景：保存物品状态与周目进度

适用：物品品质/升级、一次性奖励、机器进度、每日计数、角色排期、独立数据文件或存档备份。先确定状态属于物品实例、存档槽、周目、游戏日还是全局偏好。

## 选保存位置

| 状态 | 优先位置 | 注意 |
| --- | --- | --- |
| 某件物品的品质/升级 | 游戏可保存的物品 tags/字段 | 带命名空间键和格式版本，复制物品时核对是否复制状态 |
| 周目进度与待交付奖励 | 游戏支持的 `PlayerStore.modData` 或等效存档扩展点 | 用存档槽 + 周目 ID 隔离，缺字段与旧版本回退 |
| 用户范围设置 | MelonPreferences 或项目配置 | 不存一次性发放记录、未完成交易等周目状态 |
| 必须独立的文件 | 项目约定的 UserData 路径 | 文件名含槽/周目身份，先验证新内容再替换旧内容 |

## 生命周期

新局生成初始状态；读档迁移/恢复；保存前序列化并检查可写性；切换存档槽和场景时清空旧对象缓存。格式中写 `version`，读取时分别处理旧版、缺字段、损坏和未来版本。不要因为加载失败就自动覆盖用户原文件。

一次性行为记录“待执行 → 已写入游戏结果 → 已确认完成”，失败时保留可重试状态。跨游戏存档与外部文件的写入没有共同事务；设计保存顺序和崩溃后对账，最好让动作幂等。

## 备份

备份应在游戏确认保存完成后执行，按槽位与日期命名，设置有限保留数。复制前确认源文件属于当前槽位，备份完成后才能更新“当日已备份”标记。恢复前检查备份完整性并保留原存档副本。

## 验证

用可丢弃存档检查保存重载、换槽、新局、旧格式和部分失败；对物品核对标签与实际效果，对一次性奖励核对库存/现金而不是只核对标记。无法安全测试时明确未做运行验证。
